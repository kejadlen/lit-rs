See [AGENTS.md](AGENTS.md) for how to work in this repository.

The one thing to internalize first: `src/` is generated from the literate
sources in `lit/*.md`. Edit `lit/*.md` and run `just tangle` — never edit
`src/` by hand.
